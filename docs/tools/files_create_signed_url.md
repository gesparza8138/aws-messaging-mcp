<!-- generated by cmd/gendocs; do not edit -->

# `files_create_signed_url`

Sign (or re-sign) a shared object into a CloudFront download link, optionally IP-restricted; requires msg/files:write.

## Input schema

```json
{
  "additionalProperties": false,
  "properties": {
    "DateLessThan": {
      "description": "Absolute RFC 3339 expiry",
      "type": "string"
    },
    "ExpiresIn": {
      "description": "ISO-8601 duration; exactly one of ExpiresIn/DateLessThan",
      "type": "string"
    },
    "IpAddress": {
      "description": "Optional CIDR; produces a custom policy restricted to it",
      "type": "string"
    },
    "Key": {
      "description": "Object key, e.g. shared/\u003cuuid\u003e/video.mp4",
      "type": "string"
    }
  },
  "required": [
    "Key"
  ],
  "type": "object"
}
```

## Output schema

```json
{
  "additionalProperties": false,
  "properties": {
    "ExpiresAt": {
      "type": "string"
    },
    "PolicyType": {
      "type": "string"
    },
    "ServerMetadata": {
      "additionalProperties": false,
      "properties": {
        "dry_run": {
          "type": "boolean"
        },
        "guardrails": {
          "items": {
            "additionalProperties": false,
            "properties": {
              "allowed": {
                "type": "boolean"
              },
              "name": {
                "type": "string"
              },
              "reason": {
                "type": "string"
              }
            },
            "required": [
              "name",
              "allowed"
            ],
            "type": "object"
          },
          "type": [
            "null",
            "array"
          ]
        }
      },
      "required": [
        "guardrails",
        "dry_run"
      ],
      "type": "object"
    },
    "SignedUrl": {
      "type": "string"
    }
  },
  "required": [
    "ServerMetadata"
  ],
  "type": "object"
}
```
